box_size: 200
neighbor_radius: 200
prox_dist_max: 60

# Entity data definitions
entities_data:
  EntitySubTypes:
    - agents
    - objects

  Entities:
    agents:
      type: AGENT
      num: 1
      existing: 1
      color: blue
      selective_behaviors:
        aggr:
          beh: MANUAL
          sensed: 
            - agents
            - objects
      
    objects:
      type: OBJECT
      num: 8
      color: red
