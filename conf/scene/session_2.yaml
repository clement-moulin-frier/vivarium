box_size: 200
neighbor_radius: 200
prox_dist_max: 60

# Entity data definitions
entities_data:
  EntitySubTypes:
    - AGENTS
    - OBJECTS

  Entities:
    AGENTS:
      type: AGENT
      num: 1
      color: blue
      diameter: 10
      selective_behaviors:
        aggr:
          beh: MANUAL
          sensed: 
            - AGENTS
            - OBJECTS
      
    OBJECTS:
      type: OBJECT
      num: 13
      color: orange
      diamter: 8
